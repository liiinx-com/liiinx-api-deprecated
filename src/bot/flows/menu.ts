const NEW_LINE = "\n";

const randomIntBetween = ({start = 0, end=0}) => {
    return 0;
}

const greetingMessage = ({name}, {randomizer}) => {
    const repo = [`Hi ${name}!`]
    const messageIndex = randomizer ? randomizer({end: repo.length}) : 0
    
}

const mainMenu = async () => {};
